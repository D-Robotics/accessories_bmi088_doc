# Algolia Admin API Key 或 Write API Key（仅本地使用，勿提交）
# Write Key 需具备 addObject / deleteObject / settings（或 editSettings）等写权限
# CI：在 GitHub → Settings → Secrets and variables → Actions 配置同名 Secret
ALGOLIA_ADMIN_API_KEY=

# 可选覆盖（默认与本项目线上配置一致）
# ALGOLIA_APP_ID=1VU781LYTV
# ALGOLIA_INDEX_NAME=accessories_bmi088_doc
# ALGOLIA_SITE_URL=https://developer.d-robotics.cc/accessories_bmi088_doc/
